const mod = require("./mod");       //print the console log
//console.log(mod);
//console.log(mod([6,34]));
// console.log("index");

// console.log(mod.name)



//COMMAND
// npm
// npm init  (created pakage.json) && (This folder is initialised as node package && keep on adding new installed dependencies)
// npm (un)install express
// npm install (if node module is unistalled)
// npm i
// npm install slugift@1.3.5
           // maj.minor.batch(bug fix--batch)
           //1.0.0
           // 1.0.1(bug fix)
           // 1.1.0(new features)
           // 2.0.0 (remove old package)

// develop. dependency not for the productions
// npm install nodemon --save-dev
// npm install nodemon --global
// nodemon file(auto-server)
// npm i browserify
// ^(in version) for exact version ///  ~ for latest
// npm view nodemon version   

//POSTMAN-- see response(through url,custom header)
//EXPRESS--custom backend(routing)(if-else),functions